export type TChip = {
  background: string;
  title: string;
  icon: boolean;
};

export type TCard = {
  info?: string;
  title: string;
  description: string;
  img: string;
  date:string;
  time:string;
};
